registry=https://nexus.online.tkbbank.ru/repository/npmjs/
fund=false
audit=false

# TODO: убрать после того, как DevOps заменит $CERT в CI variables на root CA
# (сейчас там лежит leaf-cert самого Nexus — CN=repository.transcapital.com,
# issuer=TCB-ENT-CA). Нужен именно TCB-ENT-CA в PEM.
strict-ssl=false
